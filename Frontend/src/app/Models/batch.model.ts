export class Batch {

    // BatchId: string;
    // CreatedBy: string;
    // Year: number;
    // StartDate: string;
    // EndDate: string;
    // CompanyCount: number;

    data: [
        {
            BatchId: string,
            CreatedBy: string,
            Year: number,
            StartDate: string,
            EndDate: string,
            CompanyCount: number
        }
    ]

}

